# Runtime dependencies
#
# Keep release families intentionally bounded.  Exact acceptance should be
# recorded by running scripts/verify_windows.py on the target Windows machine
# before shipping a release.
PySide6>=6.10,<6.12
pywin32>=311,<313
requests>=2.32,<2.35
click>=8.1,<8.6
keyring>=25,<26
